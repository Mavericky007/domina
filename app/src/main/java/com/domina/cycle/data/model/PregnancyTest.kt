package com.domina.cycle.data.model

/** Result of a logged home pregnancy (hCG) test. A POSITIVE is the strong signal for pregnancy mode. */
enum class PregnancyTest { NOT_TESTED, NEGATIVE, POSITIVE }
